import Header from "./Header";

const Hero = () => {
  return (
    <div>
      <Header />
      <section className="font-sans bg-gray-50 text-white min-h-screen">
        
      </section>
    </div>
  );
};

export default Hero;
